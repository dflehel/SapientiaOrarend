<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="ro.sapientia.ms.sapientiaorarend">

    <uses-sdk tools:overrideLibrary="me.aflak.libraries"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".Activity.LoginScreen"
                android:label="@string/title_activity_main_screen">
        </activity>
        <activity android:name=".Activity.SignUpScreen"/>
        <activity android:name=".Activity.MainScreen"/>
        <activity android:name=".Activity.MENU"/>
        <activity android:name=".Activity.Map"/>

        <service android:name=".Services.DatabaseListening"/>

        <activity android:name=".Activity.activity_send_message"/>
        <activity android:name=".Activity.SettingsScreen">
        </activity>
        <activity android:name=".Activity.MessageDisplay">
        </activity>
        <activity
                android:name=".Activity.SplashScreen"
                android:theme="@style/Nobar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Activity.CheckPassword">
        </activity>
    </application>

</manifest>