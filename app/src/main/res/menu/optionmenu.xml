<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
>

    <item android:id="@+id/kereses"
          android:title="@string/options_screen_search"
          app:showAsAction="always"/>
    <item android:id="@+id/switchbetweenweeks"
          android:title="@string/options_screen_second"
          app:showAsAction="always"/>
    <item android:id="@+id/menu"
          android:title="@string/options_screen_food"
          app:showAsAction="always"/>

    <item android:id="@+id/uzenet"
          android:title="@string/options_screen_message"
          app:showAsAction="always"/>


</menu>